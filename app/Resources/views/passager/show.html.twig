{% extends "/layout.html.twig" %}

{% block body %}
    <div class="container mx-auto bg-dark mt-r shadow p-3 mb-5 bg-white rounded">
        <h1></h1>
        <h2>voici votre reservation</h2>


    <table class="table table-striped table-dark w-50 mx-auto shadow p-3 mb-5 rounded">
        <tbody>
            <tr>
                <th class="text-center">Id</th>
                <td>{{ passager.id }}</td>
            </tr>
            <tr>
                <th class="text-center">Reservation</th>
                <td>voiture: {{ passager.conducteur.typevoiture }}<br>nombre de place(s): {{ passager.conducteur.nbplaces}}<br>nom: {{ passager.conducteur.author}}</td>
            </tr>
        </tbody>
    </table>

    <ul class="nav">
        <li class="nav-items">
            <a class="mr-5 btn btn-sm btn-success" href="{{ path('passager_index') }}">Back to the list</a>
        </li>
        <li class="nav-items">
            <a class="mr-5 btn btn-sm btn-success" href="{{ path('passager_edit', { 'id': passager.id }) }}">Edit</a>
        </li>
        <li class="nav-items">
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
    </div>
{% endblock %}
