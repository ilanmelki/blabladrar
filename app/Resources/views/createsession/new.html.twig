{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créez votre session de covoiturage</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Confirmer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('createsession_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
